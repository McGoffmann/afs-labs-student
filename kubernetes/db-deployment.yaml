apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-deployment
  namespace: mhoffmann
  labels:
    app: db
spec:
  replicas: 1
  strategy:
    type: Recreate
  minReadySeconds: 20
  selector:
    matchLabels:
      app: db
  template:
    metadata:
      labels:
        app: db
    spec:
      containers:
      - envFrom:
        - secretRef:
            name: db-secret
        name: db-deployment
        image: registry.dev.afsmtddso.com/mhoffmann-harbor-project/db:e485130-1077c8d
        ports:
        - containerPort: 5432
